#Function is used to creat an index(or i) from a dictionary, the second line is a test if the value is odd.

#for i, key in enumerate(splice_positions):
    #if i % 2 == 1:
       #splice_positions[key] += 1 


#Function is using int to convert strings to integers, split to add commas between numbers, list comprehension with a four loop

#def string_of_integers_into_list_of_integers(specified_string):
    #list_of_integers = [int(num) for num in specified_string.split()]
    #return list_of_integers 
